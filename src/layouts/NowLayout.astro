---
// Vendored from pages/posts/[...slug].astro
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title}>
  <article
    class="flex-1 mt-24 md:mt-32 mb-12 md:mb-24 prose-mine dark:prose-invert lg:prose-lg max-w-2xl mx-auto"
  >
    <h1>{frontmatter.title}</h1>
    <p class="text-secondary mb-8">
      Last updated {new Date(frontmatter.lastUpdated).toLocaleDateString(
        "en-US",
        {
          year: "numeric",
          month: "long",
          day: "numeric",
        }
      )}
    </p>
    <a class="text-secondary font-normal underline" href="https://nownownow.com/about" target="_blank"
      >(What is this?)</a
    >
    <slot />
  </article>
</Layout>
