---
import Layout from "../../layouts/Layout.astro";

export async function getStaticPaths() {
  const allPosts = Object.values(
    import.meta.glob("./_posts/*.md", { eager: true })
  );
  return allPosts.map((post: any) => {
    const datePosted = new Date(post.frontmatter.datePosted);
    const postSlug = post.file.split("/").pop().split(".")[0]; // Use filename as slug
    return {
      params: {
        slug: `${datePosted.getFullYear()}/${datePosted.getMonth() + 1}/${datePosted.getDate()}/${postSlug}`,
      },
      props: { post },
    };
  });
}

const { post } = Astro.props;
const datePosted = new Date(post.frontmatter.datePosted).toLocaleDateString(
  "en-US",
  {
    year: "numeric",
    month: "long",
    day: "numeric",
  }
);
---

<Layout title={post.frontmatter.title}>
  <article
    class="flex-1 mt-24 md:mt-32 prose-mine dark:prose-invert lg:prose-lg max-w-2xl mx-auto"
  >
    <h1 class="!mb-6">{post.frontmatter.title}</h1>
    <p class="text-secondary !mt-3 !mb-0">
      {post.frontmatter.description}
    </p>
    <p class="text-secondary !mt-2 !mb-12">{datePosted}</p>
    <post.Content />

    <hr class="!my-24 border-tertiary w-30 mx-auto" />

    <div>
      <p>
        Something resonates?
        <a
          class="text-secondary font-normal underline"
          href="mailto:hello@andyyoon.co">Send me an email.</a
        >
      </p>
      <a
        class="text-secondary font-normal underline"
        href="/#posts">← See all posts</a
      >
    </div>
  </article>
</Layout>
